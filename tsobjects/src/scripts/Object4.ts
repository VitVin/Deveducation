const string1 = 'sparrow';
const str: string [] = string1.split('').sort();
const object: {} = {};
str.forEach((a, i) => {
    if (object[str[i]] = object[str[i]]) {
        object[str[i]] += 1;
    } else {
        object[str[i]] = 1;
    }
});
console.log(object);
