var string1 = 'sparrow';
var str = string1.split('').sort();
var object = {};
str.forEach(function (a, i) {
    if (object[str[i]] = object[str[i]]) {
        object[str[i]] += 1;
    }
    else {
        object[str[i]] = 1;
    }
});
console.log(object);