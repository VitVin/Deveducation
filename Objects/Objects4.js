var string = 'sparrow';
let str = string.split('').sort()
    object = {};
str.forEach(function (a, i) {
if (object[str[i]] = object[str[i]]) {
    object[str[i]] += 1
}
else
{
    object[str[i]] = 1
}
});

console.log(object);